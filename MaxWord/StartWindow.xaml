<Window x:Class="MaxWord.StartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:dd="clr-namespace:GongSolutions.Wpf.DragDrop;assembly=GongSolutions.Wpf.DragDrop"
        xmlns:local="clr-namespace:MaxWord"
        mc:Ignorable="d"
        Title="" Height="450" Width="800"
        Icon="MaxWord\Logo\icon.ico"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <Style x:Key="BaseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="BorderBrush" Value="#dc6281"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,5"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="5"
                                    SnapsToDevicePixels="True">
                                <ContentPresenter x:Name="contentPresenter"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  Margin="{TemplateBinding Padding}"
                                                  RecognizesAccessKey="True"/>
                            </Border>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <!-- Наведение мыши -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2f063e"/>
                                <Setter TargetName="contentPresenter" Property="TextElement.Foreground" Value="White"/>
                            </Trigger>

                            <!-- Отключенное состояние -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                                <Setter TargetName="contentPresenter" Property="TextElement.Foreground" Value="Gray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>



    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions >
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="1" Background="#2D2D30"/>
            <Border Grid.Row="0" Grid.Column="0" Background="#2D2D30">
                <Image Source="C:\Users\Admin\source\repos\MaxWord\MaxWord\Logo\logo.png" Width="40" Height="40"/>
            </Border>
            <TextBlock Text="MaxWord" TextAlignment="Center" Foreground="White" Margin="34,16,706,4" Grid.ColumnSpan="2"/>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions >
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>


            <ListBox Grid.Row="2" ItemsSource="{Binding Documents}"  Background="#2D2D30" Margin="20">  <!--SelectedItem="{Binding SelectedStudent}"-->
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <WrapPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding FileName}" Margin="10" Foreground="White"/>
                            <TextBlock Text="{Binding FormString}" Margin="10" Foreground="White"/>
                            <TextBlock Text="{Binding ModifiedDate}" Margin="10" Foreground="White"/>
                        </WrapPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!--<Border Grid.Row="2" Background="#2D2D30" Margin="20" Padding="10">
                <TextBlock x:Name="SelectedProjectText" Text="Проект не выбран" Foreground="White"/>
            </Border>-->




            <Button Grid.Column="1" Content="Create File" HorizontalAlignment="Left" Margin="129,39,0,0" VerticalAlignment="Top" Height="41" Width="196" Style="{StaticResource BaseButtonStyle}" Foreground="White" Click="Button_CreateFile"/>
            <Button Grid.Column="1" Content="Open File" HorizontalAlignment="Left" Margin="129,100,0,0" VerticalAlignment="Top" Height="41" Width="196"  Style="{StaticResource BaseButtonStyle}" Foreground="White" Click="Button_OpenFile"/>
            <Button Grid.Column="1" Content="Open Folder" HorizontalAlignment="Left" Margin="129,159,0,0" VerticalAlignment="Top" Height="41" Width="196"  Style="{StaticResource BaseButtonStyle}" Foreground="White" Click="Button_OpenFolder"/>
           

        </Grid>

    </Grid>
</Window>
